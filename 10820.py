# Analy_ Str


"""
try:
 while 1:i=input();print(*[len([*filter(j,i)])for j in[str.islower,str.isupper,str.isdigit,str.isspace]])
except:0
"""

try:
 while i:=input():print(*[len([*filter(j,i)])for j in[str.islower,str.isupper,str.isdigit,str.isspace]])
except:0