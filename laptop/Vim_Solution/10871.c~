// Num less than X

#include <stdio.h>

int list[10000] = {0, };

int main(void){
	int n, x;
	scanf("%d %d", &n, &x);

	for(int i = 0; i < n; i++){
		scanf("%d", &list[i]);
	}

	for(int i = 0; i < n; i++){
		if(list[i] < x) printf("%d ", list[i]);
	}
	return 0;
}
