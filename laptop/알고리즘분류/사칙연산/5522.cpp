// Card Game

#include <bits/stdc++.h>

using namespace std;

int main(){
    int sum;
    int a,b,c,d,e;
    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;
    cin >> e;
    sum=a+b+c+d+e;
    cout << sum;
    return 0;
}